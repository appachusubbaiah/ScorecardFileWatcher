package com.ocwen.repositories;

import org.springframework.data.jpa.repository.JpaRepository;

import com.ocwen.models.ASIT_QUALITY_YIELD;

public interface ASIT_QUALITY_YIELD_Repository extends JpaRepository<ASIT_QUALITY_YIELD, Long> {

}
